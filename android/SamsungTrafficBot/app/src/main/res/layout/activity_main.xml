<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- WebView fills the entire screen -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Config panel overlays the top — hidden when running -->
    <LinearLayout
        android:id="@+id/configPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFFFF"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Samsung Traffic Bot"
            android:textSize="18sp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/etDeviceName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="기기명 예: z1 또는 z1-1"
            android:inputType="text" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="4dp"
            android:paddingBottom="6dp"
            android:text="네이버 계정은 관리자 페이지에서 기기별로 할당됩니다."
            android:textColor="#FF666666"
            android:textSize="12sp" />

        <EditText
            android:id="@+id/etServerUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Vercel server URL"
            android:inputType="textUri"
            android:visibility="gone" />

        <EditText
            android:id="@+id/etNaverId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Naver ID"
            android:inputType="text"
            android:visibility="gone" />

        <EditText
            android:id="@+id/etNaverPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Naver password"
            android:inputType="textPassword"
            android:visibility="gone" />

        <CheckBox
            android:id="@+id/cbShowNaverPassword"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="비밀번호 표시"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnRunA"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="기기명 저장 후 시작" />

    </LinearLayout>

    <!-- Status bar overlay at the bottom — visible when running -->
    <LinearLayout
        android:id="@+id/statusBar"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_gravity="bottom"
        android:background="#CC000000"
        android:orientation="vertical"
        android:padding="8dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvLog"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:ellipsize="end"
            android:lines="5"
            android:textColor="#FFFFFFFF"
            android:textSize="11sp" />

    </LinearLayout>

</FrameLayout>
